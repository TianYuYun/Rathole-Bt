# Changelog

## 2.2.0 - 2026-09-10

- 按当前宝塔/aaPanel Modern UI 重新构建插件界面，不再使用 V2 的通用 SaaS 风格卡片。
- 采用官方 `BtTabsModal` 的左侧导航结构：Modern Tab 高 32px、圆角 6px、内容区圆角 8px、左侧分隔线。
- 对齐当前官方组件尺寸：Input/Button 中号 32px、Button 小号 26px、表单标签 12px、按钮圆角 6px。
- 表格对齐官方 DataTable：表头 padding 8px、单元格 padding 6px 8px，并读取面板当前主题变量。
- 删除插件内部重复的大标题；直接配合宝塔外层设置弹窗。
- 新增插件自有 Modern Confirm，按当前 `BtModal` 的 53px 标题栏、10px 圆角、24px 关闭按钮和底部操作区实现。
- 新增插件自有居中消息提示，按当前 `BtMessage` 的 360px 最大宽度与 16/20/15px 间距实现；安装与服务操作支持 loading 状态。
- 服务管理页改为官方软件设置风格：状态、启动/停止/重启、版本、模式、配置状态、开机自启集中管理。
- 开机自启改为 28x16 的 Modern Switch，滑块 12x12。
- 连接配置、穿透规则、运行日志、版本管理独立为左侧页签。
- 增强 Noise 密钥更换确认、停止服务确认、开发版安装确认。
- 继续保持官方第三方插件兼容入口，不劫持宝塔私有 Vue/Vite 模块。
- 保留并增强 GitHub 在线更新能力：检查 `update.json`、从版本 Tag 下载文件、更新前备份、Python/Shell/版本一致性校验与失败回滚。

## 2.0.0 - 2026-09-10

- 修复空配置导致 systemd 无限失败重启。
- 地址与端口拆分。
- 客户端/服务端模式分离。
- Noise 自动密钥与配置校验。
